choice=int(raw_input("Enter 0 for server 1 for client"))
if choice==0:
    print "hello World"    
else:
    ip=raw_input("Enter the ip address of server")
    port=raw_input("Enter the port number of the server")

